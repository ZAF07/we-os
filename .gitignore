logs/
# pnpm's project-local store fallback (a package cache, not project content).
# It lands next to whichever package root pnpm resolved, so ignore it anywhere.
.pnpm-store/
# Playwright run output
test-results/
web/test-results/
web/playwright-report/
web/.auth/
# Environment files anywhere in the repo — real credentials.
# `.env.example` templates are tracked deliberately.
.env
.env.*
!.env.example
!*.env.example
