# Performance Knowledge

Read by the `performance-marketing` subagent.

Holds: channel playbooks, KPI models (Business / Marketing / Creative tiers), budgeting frameworks, and optimization heuristics.

Add each as its own `.md` file (e.g. `channel-playbooks.md`, `kpi-targets.md`, `budget-model.md`, `optimization-rules.md`).

<!-- TODO: add your channel benchmarks, KPI targets, budget model, and optimization rules. -->
