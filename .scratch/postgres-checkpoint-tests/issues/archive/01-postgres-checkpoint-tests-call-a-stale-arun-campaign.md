# 01 — The Postgres checkpoint tests call a stale `arun_campaign` signature

Status: completed
Type: bug

## Context

Three tests in `agent-harness/tests/test_postgres.py` fail as soon as the
Postgres suite is actually run:

- `test_a_run_checkpoint_outlives_the_process_that_wrote_it`
- `test_a_run_halted_at_a_gate_is_approvable_after_a_restart`
- `test_clearing_a_campaigns_threads_removes_its_durable_state`

Each calls `arun_campaign(settings, TENANT, SLUG, stage=..., checkpointer=...)`
and gets:

    TypeError: arun_campaign() missing 3 required keyword-only arguments:
    'document_store', 'deliverable_store', and 'usage_ledger'

`arun_campaign` (`graph/runner.py:502-517`) takes `document_store`,
`deliverable_store` and `usage_ledger` as required keyword-only arguments —
dependency injection replaced whatever these tests were written against — and
the three call sites were never updated.

## Why it went unnoticed

The whole module is opt-in: it skips unless `MARKETING_OS_TEST_POSTGRES=1` is
set, and it needs Docker. `uv run pytest` reports 575 passed / 84 skipped and
stays green, so the durability guarantees these tests exist to protect —
a checkpoint outliving its process, a gate still approvable after a restart,
abandonment reaching the database rather than a dict — are currently unverified.

## Reproduce

    cd agent-harness
    MARKETING_OS_TEST_POSTGRES=1 uv run pytest tests/test_postgres.py -q
    # 3 failed, 27 passed

Confirmed present on `main` at 33c1527, independent of any in-flight branch.

## What should happen

- The three call sites pass the stores `arun_campaign` now requires, using
  whatever the module's other tests already build for the same purpose.
- The suite passes with `MARKETING_OS_TEST_POSTGRES=1`.
- Worth deciding separately whether an opt-in suite that can rot unseen should
  run in CI, since that is what let this sit.

## Acceptance criteria

- [x] `MARKETING_OS_TEST_POSTGRES=1 uv run pytest tests/test_postgres.py` passes.
- [x] The three tests assert the same durability behaviour they were written for,
      not a narrowed version of it.

## Agent brief

> *This was generated by AI during triage.*

**Triaged 2026-09-08.** Category `bug`, state `ready-for-agent`. Verified against a
real container before writing this.

### Verification

Confirmed on `main`, working tree clean, Docker running:

```
cd agent-harness
MARKETING_OS_TEST_POSTGRES=1 uv run pytest tests/test_postgres.py -q
# 3 failed, 29 passed in 6.26s
```

Same three tests, same `TypeError`. Note the count: **29 passed, not 27** — the
suite grew since this was filed. The Context section above is otherwise accurate.

### Correction to the Context section

The issue reads as though DI landed and these call sites were missed in the same
change. The history says otherwise:

- DI landed in `d6f90aa` — 2026-09-06 23:18
- `test_postgres.py` was last edited in `0ad624f` — 2026-09-08 10:45

The file was edited **two days after** the break and the stale calls still went
unnoticed, because `uv run pytest` skips the whole module. Not a forgotten call
site during a refactor — a file being actively worked on while broken, invisibly.

### Why this matters more than "three tests are red"

These three are the **only** coverage of durable checkpointing anywhere in the
suite. Searched by concept, not wording:

- `tests/test_runner_checkpoint.py` covers the checkpointer *seam*, but with
  `MemorySaver`. Its module docstring explicitly defers the durable case: "The
  durable Postgres checkpointer is covered against a real database in
  `test_postgres.py`."
- `tests/test_run_abandonment.py` exercises `clear_campaign_threads`, in memory only.
- `AsyncPostgresSaver` appears in exactly two places in the repo: the adapter at
  `src/marketing_os/adapters/postgres/__init__.py:83`, and these three tests.

So what is currently unverified is what `test_postgres.py`'s own header calls
"the whole reason Postgres is a hard prerequisite for approval gates" — a
checkpoint outliving its process, a gate still approvable after a restart, and
abandonment reaching the database rather than a dict.

Do not narrow these tests to make them pass. Each must still prove the durability
behaviour it was written for.

### The fix

`prototype_adapters(settings.root)` in `tests/conftest.py:161` already returns
exactly the keyword arguments `arun_campaign` now requires
(`document_store`, `deliverable_store`, `usage_ledger`, `checkpointer`).
`tests/test_runner_checkpoint.py:36` shows the established idiom:

```python
adapters = {**prototype_adapters(settings.root), "checkpointer": saver}
```

Apply that at the three call sites, keeping each test's existing intent:

1. **`test_a_run_checkpoint_outlives_the_process_that_wrote_it`** (line ~365) —
   plain splat with `checkpointer=writer`.
2. **`test_a_run_halted_at_a_gate_is_approvable_after_a_restart`** (lines ~394
   and ~402) — **careful here.** This test passes its own
   `deliverable_store=versions` (an `InMemoryDeliverableStore` it asserts on at
   the end via `versions.latest(...)`). The splat must not overwrite it. Both
   `arun_campaign` calls in this test need the override preserved, and the second
   also keeps its `resume=Command(...)`.
3. **`test_clearing_a_campaigns_threads_removes_its_durable_state`** (line ~428) —
   plain splat with `checkpointer=saver`.

Each test already takes the `settings` fixture, so `settings.root` is in scope.
Note `prototype_adapters` sets `checkpointer: None`; every call site here
overrides it with a real `AsyncPostgresSaver`, which is the point.

### Out of scope — decided, do not do it

The issue's third bullet ("worth deciding separately whether an opt-in suite that
can rot unseen should run in CI") is **resolved: no CI.**

Investigated during triage: `.github/workflows/` contains one workflow,
`contract.yml`, which lints the OpenAPI spec and runs a mock server. It is
path-filtered to `contracts/**`, so nothing in `agent-harness/` runs in CI —
not `pytest`, not `ruff`, not `mypy`. `agent-harness/Makefile:16` has a
`test-postgres` target commented "Use this in CI", which never happened.

**Maintainer decision, 2026-09-08:** no GitHub CI while this is an MVP — speed
over ceremony. The suites are run manually before pushing instead, using the
existing make targets. Do not add a workflow file as part of this fix.

The documentation half of that decision is tracked separately as issue
[02](02-run-the-full-suite-before-pushing.md). This issue is the test fix only.

### Acceptance criteria

Unchanged from above, plus:

- [x] `MARKETING_OS_TEST_POSTGRES=1 uv run pytest tests/test_postgres.py -q` passes
      (expect 32 passed).
- [x] The three tests assert the same durability behaviour they were written for.
      In particular `test_a_run_halted_at_a_gate_is_approvable_after_a_restart`
      still asserts `versions.latest(TENANT, SLUG, "campaign-strategy") is not None`
      against its own `InMemoryDeliverableStore`.
- [x] `make check` passes (ruff, ruff format, mypy, pytest).
- [x] No `.github/workflows/` file added.

### Where the work is

- `agent-harness/tests/test_postgres.py` — the three call sites. Nothing else.

No production code changes. `arun_campaign`'s signature is correct as it stands;
the tests are what drifted.

## Completion

- Completed: 2026-09-08
- Commits:
  - `2c57669` — pass the stores at the three call sites via the
    `prototype_adapters` splat, keeping each test's intent.
  - `dcfd255` — code review follow-up: one splat idiom across all three sites.
- Evidence: `MARKETING_OS_TEST_POSTGRES=1 uv run pytest tests/test_postgres.py -q`
  → **32 passed** (was 3 failed / 29 passed). `make check` → 576 passed / 86
  skipped. `make test-postgres` → **662 passed, 0 skipped**.
- The three tests keep the durability behaviour they were written for: zero
  `assert` lines were deleted, and
  `test_a_run_halted_at_a_gate_is_approvable_after_a_restart` still asserts
  `versions.latest(TENANT, SLUG, "campaign-strategy") is not None` against its
  own `InMemoryDeliverableStore` (`tests/test_postgres.py:412`).
- No production code changed; no `.github/workflows/` file added.
